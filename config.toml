# Binds on all interfaces so containers and PaaS can reach the app.
# Override with env: STREAMLIT_SERVER_PORT, STREAMLIT_SERVER_ADDRESS

[server]
headless = true
address = "0.0.0.0"
port = 8501

[browser]
gatherUsageStats = false
